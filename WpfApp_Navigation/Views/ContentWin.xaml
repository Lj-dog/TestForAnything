<Window
    x:Class="WpfApp_Navigation.ContentWin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp_Navigation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:WpfApp_Navigation.VMs"
    Title="ContentWin"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance Type={x:Type vm:ContentVM}}"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/ContentsStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--  Content显示数据DataTemplate  -->
        <StackPanel Grid.Row="0">
            <Button Command="{Binding NextCommand}" Content="Next" />
            <Button Command="{Binding PreviousCommand}" Content="Previous" />
        </StackPanel>
        <ContentControl Grid.Row="1" Content="{Binding ShowContent}" />
    </Grid>
</Window>
