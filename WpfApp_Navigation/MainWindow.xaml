<Window
    x:Class="WpfApp_Navigation.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp_Navigation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                <Style.Setters>
                    <Setter Property="Width" Value="120" />
                    <Setter Property="Height" Value="60" />
                </Style.Setters>
            </Style>
        </Grid.Resources>
        <Button Grid.Column="0" Content="NavigationWin"  Click="NavigationWinOpen" />
        <Button Grid.Column="1" Content="FrameWin" Click="FrameWinOpen" />
        <Button Grid.Column="2" Content="ContentWin" Click="ContentWinOpen" />
    </Grid>
</Window>
