<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:My="clr-namespace:WpfAppDictionary.MyControl"
    xmlns:m="clr-namespace:WpfAppDictionary.Models">

    <DataTemplate DataType="{x:Type m:Card}">
        <Border
            x:Name="border"
            Padding="5"
            BorderBrush="Gray"
            BorderThickness="1"
            CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock
                    x:Name="txtName"
                    VerticalAlignment="Center"
                    FontSize="18"
                    FontWeight="Black"
                    Text="{Binding Name}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <!--不要写在控件里，需要写在外层DataTemplate处才能识别出VM里的属性以及同级其他控件名称-->
                                <DataTrigger Binding="{Binding MP}" Value="ZERO">
                                    <Setter Property="Foreground" Value="LightBlue" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="{Binding MPValue}" />
                    <TextBlock Text="{Binding Name}" />
                    <TextBlock Text="{Binding PriorityValue}" />
                    <TextBlock Text="{Binding Skill_Description}" />
                    <TextBlock Text="{Binding V_Condition}" />
                </StackPanel>
            </Grid>
        </Border>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding MPValue}" Value="ZERO">
                <Setter TargetName="txtName" Property="Foreground" Value="LightBlue" />
            </DataTrigger>
            <DataTrigger Binding="{Binding MPValue}" Value="-1">
                <Setter TargetName="txtName" Property="Foreground" Value="LightGreen" />
            </DataTrigger>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter TargetName="border" Property="BorderBrush" Value="yellow" />
            </Trigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <DataTemplate DataType="{x:Type m:Student}">
        <Border
            x:Name="border"
            Padding="5"
            BorderBrush="Blue"
            BorderThickness="5"
            CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock
                    x:Name="txtName"
                    VerticalAlignment="Center"
                    FontSize="18"
                    FontWeight="Black"
                    Text="Student" />
                <StackPanel Grid.Column="1">
                    <TextBlock Text="{Binding Id}" />
                    <TextBlock Text="{Binding Age}" />
                    <TextBlock Text="{Binding Sex}" />
                </StackPanel>
            </Grid>
        </Border>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding Sex}" Value="MALE">
                <Setter TargetName="txtName" Property="Foreground" Value="LightBlue" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Sex}" Value="FEMALE">
                <Setter TargetName="txtName" Property="Foreground" Value="LightGreen" />
            </DataTrigger>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter TargetName="border" Property="BorderThickness" Value="10" />
            </Trigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <DataTemplate DataType="{x:Type m:Teacher}">
        <Border
            x:Name="border"
            Padding="5"
            BorderBrush="Red"
            BorderThickness="10"
            CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock
                    x:Name="txtName"
                    VerticalAlignment="Center"
                    FontSize="18"
                    FontWeight="Black"
                    Text="Teacher" />
                <StackPanel Grid.Column="1">
                    <TextBlock Text="{Binding Salary}" />
                    <TextBlock Text="{Binding Age}" />
                    <TextBlock Text="{Binding Sex}" />
                </StackPanel>
            </Grid>
        </Border>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding Sex}" Value="MALE">
                <Setter TargetName="txtName" Property="Foreground" Value="LightBlue" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Sex}" Value="FEMALE">
                <Setter TargetName="txtName" Property="Foreground" Value="LightGreen" />
            </DataTrigger>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter TargetName="border" Property="BorderBrush" Value="yellow" />
            </Trigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>