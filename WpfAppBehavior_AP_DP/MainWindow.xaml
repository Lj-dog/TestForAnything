<Window
    x:Class="WpfAppBehavior.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:WpfAppBehavior_AP_DP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfAppBehavior"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.Resources>
        <Style TargetType="{x:Type c:CornerButton}">
            <Style.Setters>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type c:CornerButton}">
                            <Border Background="{TemplateBinding Background}" CornerRadius="{TemplateBinding ButtonCornerRadius}">
                                <ContentPresenter />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style.Setters>
        </Style>
    </Window.Resources>
    <Grid>
        <StackPanel>
            <c:CornerButton
                x:Name="cornerbtn"
                Width="120"
                Height="60"
                Background="Purple"
                ButtonCornerRadius="6" />
            <StackPanel
                Margin="0,10"
                HorizontalAlignment="Center"
                Orientation="Horizontal">
                <Button
                    Width="60"
                    Height="30"
                    Click="Button_Click_3"
                    Content="3" />
                <Button
                    Width="60"
                    Height="30"
                    Click="Button_Click_8"
                    Content="8" />
                <Button
                    Width="60"
                    Height="30"
                    Click="Button_Click_10"
                    Content="10" />

                <Button
                    Width="60"
                    Height="30"
                    Click="Button_Click_15"
                    Content="15" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
